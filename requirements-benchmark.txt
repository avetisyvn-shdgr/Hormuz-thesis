# Isolated benchmark env #1: Chronos-2 (Amazon) + Moirai 2.0 (Salesforce).
# DEVELOPMENT CONSTRAINTS ONLY. For the citable environment, install the full
# exact `requirements-benchmark.lock.txt` into a fresh Python-3.11 venv.
# DELIBERATELY SEPARATE from the frozen core requirements.txt: the PyTorch stack
# and model weights are optional external artifacts and must never be added to
# the locked analysis pipeline (CLAUDE.md; docs/MODERN_TSFM_BENCHMARK.md).
#
# Use Python 3.11 — the foundation-model stacks do NOT support the repo's core
# Python 3.14 venv, and uni2ts/Moirai is pinned below Python 3.12.
#
#   /opt/homebrew/bin/python3.11 -m venv .venv-bench
#   .venv-bench/bin/python -m pip install -r requirements-benchmark.lock.txt
#   .venv-bench/bin/python scripts/run_tsfm_benchmark.py \
#       --model chronos2,moirai --acknowledge-benchmark-only
#
# NOTE: uni2ts pins an older torch (2.4.x). TimesFM 2.5 needs a newer torch and
# emits all-NaN under that pin, so TimesFM runs in its OWN env — see
# requirements-timesfm.txt. Chronos-2 and Moirai 2.0 coexist here cleanly.
#
# Verified working on 2026-06-18 (macOS arm64, Python 3.11.15): the resolved set
# below installed and produced real forecasts. Freeze exact versions
# (pip freeze) into the reproducibility manifest BEFORE reporting any number.

# Chronos-2 (Amazon) — DataFrame zero-shot probabilistic forecaster, native 95%.
# Pulls torch + transformers + accelerate as dependencies.
chronos-forecasting>=2.0          # verified: 2.3.0

# Moirai 2.0 (Salesforce) — Uni2TS + GluonTS, decile head (native 80% interval).
# Pulls torch (2.4.x), lightning, gluonts as dependencies.
uni2ts>=2.0                       # verified: 2.0.0
gluonts>=0.14                     # verified: 0.14.4 (installed by uni2ts)
